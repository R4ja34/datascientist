require_relative "fonction.rb"
require_relative "data_handle.rb"
handles = data

size(handles) #taille liste
element_min(handles) # element min
element_max(handles) # element max
element_predictable(handles) # choix index
find_index(handles)
length_element(handles)
start_upcase(handles)
start_downcase(handles)
sort_by_length(handles)
sort_by_az(list)
group_words_by_length(handles)
menu_first_end(handles)

